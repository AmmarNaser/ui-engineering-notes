// Generated at: 2026-09-08T07:55:07.928Z

This snippet demonstrates a simple reusable React component for a button that can handle various styles and click events, making it versatile for different use cases.

### `CustomButton.js` - Reusable Button Component

```jsx
import React from 'react';
import PropTypes from 'prop-types';

const CustomButton = ({ onClick, label, styleType }) => {
  const styles = {
    default: {
      padding: '10px 20px',
      backgroundColor: '#3498db',
      color: '#fff',
      border: 'none',
      borderRadius: '5px',
      cursor: 'pointer',
    },
    danger: {
      backgroundColor: '#e74c3c',
    },
    success: {
      backgroundColor: '#2ecc71',
    },
  };

  const combinedStyles = {
    ...styles.default,
    ...(styles[styleType] || {}),
  };

  return (
    <button style={combinedStyles} onClick={onClick}>
      {label}
    </button>
  );
};

CustomButton.propTypes = {
  onClick: PropTypes.func.isRequired,
  label: PropTypes.string.isRequired,
  styleType: PropTypes.oneOf(['default', 'danger', 'success']),
};

CustomButton.defaultProps = {
  styleType: 'default',
};

export default CustomButton;
```

### Usage Example

```jsx
import React from 'react';
import ReactDOM from 'react-dom';
import CustomButton from './CustomButton';

const App = () => {
  const handleClick = () => {
    alert('Button Clicked!');
  };

  return (
    <div>
      <CustomButton 
        onClick={handleClick} 
        label="Default Button" 
      />
      <CustomButton 
        onClick={handleClick} 
        label="Danger Button" 
        styleType="danger" 
      />
      <CustomButton 
        onClick={handleClick} 
        label="Success Button" 
        styleType="success" 
      />
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
```

### Explanation
- **Reusable Component**: The `CustomButton` can be reused with different styles and labels throughout your application.
- **Style Variants**: It supports different styles (`default`, `danger`, `success`) which can be easily extended.
- **Prop Types**: It uses PropTypes for type checking, ensuring that the required props are passed.
- **Default Props**: It sets a default style type to ensure the button renders correctly even if not specified.

This component can be easily customized and integrated into your applications, enriching your UI with consistent button styles.
