// Generated at: 2026-03-19T04:57:34.676Z

This snippet demonstrates a reusable React functional component that displays user information in a card format. It utilizes React props to make the component flexible for different user data.

### `UserCard.js` - Reusable User Card Component

```javascript
import React from 'react';
import PropTypes from 'prop-types';
import './UserCard.css'; // Assuming there's a CSS file for styling

const UserCard = ({ name, email, avatar }) => {
  return (
    <div className="user-card">
      <img src={avatar} alt={`${name}'s avatar`} className="user-card__avatar" />
      <h2 className="user-card__name">{name}</h2>
      <p className="user-card__email">{email}</p>
    </div>
  );
};

UserCard.propTypes = {
  name: PropTypes.string.isRequired,
  email: PropTypes.string.isRequired,
  avatar: PropTypes.string.isRequired,
};

export default UserCard;
```

### Usage Example

```javascript
import React from 'react';
import ReactDOM from 'react-dom';
import UserCard from './UserCard';

const App = () => (
  <div>
    <h1>User Information</h1>
    <UserCard 
      name="John Doe" 
      email="john.doe@example.com" 
      avatar="https://i.pravatar.cc/150?img=3" 
    />
  </div>
);

ReactDOM.render(<App />, document.getElementById('root'));
```

### Explanation
- **Reusable Component**: The `UserCard` component can be reused with different user data by passing props.
- **Prop Types Validation**: Utilizes `PropTypes` for type-checking props, ensuring that the required data types are provided.
- **Styling Separation**: Assumes a separate CSS file for styling, promoting better organization and cleaner code.

This User Card component can be easily integrated into any application, offering a consistent way to display user information.
