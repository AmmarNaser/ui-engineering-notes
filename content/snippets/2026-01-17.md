// Generated at: 2026-01-17T03:56:36.275Z

This snippet illustrates a reusable React component that displays a loading spinner while content is being fetched or processed. It can be easily customized to fit different design requirements.

### `LoadingSpinner.js` - Reusable Loading Spinner Component

```jsx
import React from 'react';
import PropTypes from 'prop-types';
import './LoadingSpinner.css'; // Assume CSS styles are defined in this file

const LoadingSpinner = ({ size = '40px', color = '#007BFF' }) => {
    return (
        <div
            className="loading-spinner"
            style={{
                width: size,
                height: size,
                border: `4px solid ${color}`,
                borderTopColor: 'transparent',
                borderRadius: '50%',
                animation: 'spin 1s linear infinite'
            }}
        />
    );
};

LoadingSpinner.propTypes = {
    size: PropTypes.string,
    color: PropTypes.string,
};

export default LoadingSpinner;
```

### Usage Example

```jsx
import React, { useState, useEffect } from 'react';
import LoadingSpinner from './LoadingSpinner';

const App = () => {
    const [loading, setLoading] = useState(true);
    const [data, setData] = useState([]);

    useEffect(() => {
        const fetchData = async () => {
            // Simulating a data fetch
            const response = await new Promise(res => 
                setTimeout(() => res(['Item 1', 'Item 2', 'Item 3']), 2000)
            );
            setData(response);
            setLoading(false);
        };
        fetchData();
    }, []);

    return (
        <div>
            {loading ? (
                <LoadingSpinner size="50px" color="purple" />
            ) : (
                <ul>
                    {data.map((item, index) => (
                        <li key={index}>{item}</li>
                    ))}
                </ul>
            )}
        </div>
    );
};

export default App;
```

### Explanation
- **Loading Spinner**: The `LoadingSpinner` component displays a customizable spinner while data is being loaded.
- **Customizable Size**: The spinner size can be adjusted via the `size` prop, defaulting to `40px`.
- **Customizable Color**: The spinner color can be modified using the `color` prop, with a default value of `#007BFF`.
- **PropTypes Validation**: The component uses PropTypes for prop validation, ensuring the right data types are passed.

This LoadingSpinner component is a practical tool for enhancing user experience during data loading processes.
