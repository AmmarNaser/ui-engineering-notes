// Generated at: 2026-09-26T08:24:59.428Z

This snippet demonstrates a reusable React functional component for a customizable button. It allows you to define styles and handle clicks effectively.

### `CustomButton.js` - Customizable Button Component

```jsx
import React from 'react';
import PropTypes from 'prop-types';

const CustomButton = ({ label, onClick, style, disabled }) => {
  return (
    <button
      onClick={onClick}
      style={{ padding: '10px 20px', ...style }}
      disabled={disabled}
    >
      {label}
    </button>
  );
};

CustomButton.propTypes = {
  label: PropTypes.string.isRequired,
  onClick: PropTypes.func.isRequired,
  style: PropTypes.object,
  disabled: PropTypes.bool,
};

CustomButton.defaultProps = {
  style: {},
  disabled: false,
};

export default CustomButton;
```

### Usage Example

```jsx
import React from 'react';
import ReactDOM from 'react-dom';
import CustomButton from './CustomButton';

const App = () => {
  const handleClick = () => {
    alert('Button clicked!');
  };

  return (
    <div>
      <h1>Custom Button Example</h1>
      <CustomButton 
        label="Click Me!"
        onClick={handleClick}
        style={{ backgroundColor: 'blue', color: 'white' }}
      />
      <CustomButton 
        label="Disabled Button"
        onClick={handleClick}
        disabled={true}
        style={{ backgroundColor: 'gray', color: 'darkgray' }}
      />
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
```

### Explanation
- **Reusable Component**: The `CustomButton` component can be used throughout your application with different props for various buttons.
- **Props Typing**: Utilizes PropTypes for type-checking props, ensuring correct usage and improved maintainability.
- **Custom Styles**: Allows you to pass custom styles through the `style` prop, making it flexible for design requirements.
- **Disabled State**: Supports a `disabled` state, providing users with feedback on button interactions.

This component offers an easily customizable solution for buttons in any React application.
