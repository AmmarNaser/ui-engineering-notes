// Generated at: 2026-07-22T05:52:57.514Z

This snippet demonstrates a reusable React component that fetches data from an API and displays it. It includes loading and error states to improve the user experience.

### `FetchDataComponent.js` - Reusable Data Fetching Component

```javascript
import React, { useEffect, useState } from 'react';

const FetchDataComponent = ({ url }) => {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchData = async () => {
      try {
        const response = await fetch(url);
        if (!response.ok) {
          throw new Error('Network response was not ok');
        }
        const result = await response.json();
        setData(result);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };

    fetchData();
  }, [url]);

  if (loading) return <p>Loading...</p>;
  if (error) return <p>Error: {error}</p>;

  return (
    <div>
      <h3>Fetched Data:</h3>
      <pre>{JSON.stringify(data, null, 2)}</pre>
    </div>
  );
};

export default FetchDataComponent;
```

### Usage Example

```javascript
import React from 'react';
import ReactDOM from 'react-dom';
import FetchDataComponent from './FetchDataComponent';

const App = () => {
  return (
    <div>
      <h1>Data Fetching Example</h1>
      <FetchDataComponent url="https://api.example.com/data" />
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
```

### Explanation
- **Data Fetching**: The component uses the Fetch API to retrieve data from a specified URL, managing the loading and error states appropriately.
- **Reusable Component**: It accepts a `url` prop, allowing it to be reused for different endpoints without modification.
- **Error Handling**: The component gracefully handles network errors, providing feedback to the user when data fails to load. 

This component can be easily integrated into any React application to handle API data fetching.
