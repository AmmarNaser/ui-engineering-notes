// Generated at: 2026-03-02T04:51:16.355Z

This code snippet demonstrates a reusable custom hook called `useFetch` that allows you to easily make API calls and manage loading and error states in your React applications.

### `useFetch.js` - Custom Hook for Fetching Data
```javascript
import { useState, useEffect } from 'react';

const useFetch = (url) => {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchData = async () => {
      try {
        const response = await fetch(url);
        if (!response.ok) {
          throw new Error('Network response was not ok');
        }
        const result = await response.json();
        setData(result);
      } catch (error) {
        setError(error);
      } finally {
        setLoading(false);
      }
    };

    fetchData();
  }, [url]);

  return { data, loading, error };
};

export default useFetch;
```

### Usage Example
```javascript
import React from 'react';
import useFetch from './useFetch';

const App = () => {
  const { data, loading, error } = useFetch('https://jsonplaceholder.typicode.com/posts');

  if (loading) return <div>Loading...</div>;
  if (error) return <div>Error: {error.message}</div>;

  return (
    <div>
      <h1>Posts</h1>
      <ul>
        {data.map(post => (
          <li key={post.id}>{post.title}</li>
        ))}
      </ul>
    </div>
  );
};

export default App;
```

### Explanation
- **Custom Hook**: The `useFetch` hook encapsulates the logic for fetching data, making it reusable across different components.
- **Loading State**: It provides a loading state to indicate when data is being fetched, allowing for better user experience.
- **Error Handling**: The hook includes error handling to manage any issues that arise during the fetch request.

This hook can be a foundational part of your components when working with external APIs, simplifying your code and improving maintainability.
