// Generated at: 2026-02-27T04:48:23.864Z

This code snippet demonstrates how to create a customizable button component in React. The component allows you to pass props for handling click events and styling.

### `CustomButton.js` - Reusable Custom Button Component

```javascript
import React from 'react';
import PropTypes from 'prop-types';

const CustomButton = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="custom-button">
      {label}
    </button>
  );
};

CustomButton.propTypes = {
  label: PropTypes.string.isRequired,
  onClick: PropTypes.func.isRequired,
  style: PropTypes.object,
};

CustomButton.defaultProps = {
  style: {},
};

export default CustomButton;
```

### Usage Example

```javascript
import React from 'react';
import ReactDOM from 'react-dom';
import CustomButton from './CustomButton';

const App = () => {
  const handleClick = () => {
    alert('Button clicked!');
  };

  return (
    <div>
      <h1>Hello, World!</h1>
      <CustomButton 
        label="Click Me" 
        onClick={handleClick} 
        style={{ backgroundColor: '#4CAF50', color: 'white', padding: '10px 20px', borderRadius: '5px' }} 
      />
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
```

### Explanation
- **Prop Types**: The component utilizes `PropTypes` to ensure that required props are passed, improving type safety.
- **Default Props**: It provides default values for optional props, making it versatile and easier to use in different contexts.
- **Inline Styles**: The component allows styling through props, facilitating customization without altering the core component logic.

This reusable button component can greatly simplify UI development while ensuring consistency across your application.
