// Generated at: 2026-02-05T04:51:09.776Z

This snippet demonstrates a reusable custom input component in React that can handle both text input and number input types, complete with validation and error handling.

### `CustomInput.js` - Reusable Input Component with Validation

```javascript
import React, { useState } from 'react';

const CustomInput = ({ type = 'text', label, required = false }) => {
  const [value, setValue] = useState('');
  const [error, setError] = useState('');

  const handleChange = (e) => {
    const { value } = e.target;
    setValue(value);

    // Simple validation
    if (required && !value) {
      setError(`${label} is required.`);
    } else {
      setError('');
    }
  };

  return (
    <div>
      <label>
        {label}
        <input 
          type={type} 
          value={value} 
          onChange={handleChange} 
        />
      </label>
      {error && <span style={{ color: 'red' }}>{error}</span>}
    </div>
  );
};

export default CustomInput;
```

### Usage Example

```javascript
import React from 'react';
import ReactDOM from 'react-dom';
import CustomInput from './CustomInput';

const App = () => {
  return (
    <div>
      <h1>Custom Input Example</h1>
      <CustomInput label="Name" required={true} />
      <CustomInput label="Age" type="number" />
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
```

### Explanation
- **Custom Input Component**: A reusable component that can accept different input types (text or number).
- **Validation**: Checks if the field is required and whether it has a value, displaying an error message if not.
- **State Management**: Uses `useState` to manage input value and error messages, ensuring the component is controlled and reactive.

This component can easily be customized and extended for various input requirements, making it a valuable addition to your React applications.
