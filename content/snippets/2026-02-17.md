// Generated at: 2026-02-17T04:55:49.980Z

This snippet demonstrates a reusable React component that implements a toggle switch for enabling or disabling a feature. It includes customizable labels and styles.

### `ToggleSwitch.js` - Reusable Toggle Switch Component

```jsx
import React, { useState } from 'react';

const ToggleSwitch = ({ label, initialState = false, onToggle }) => {
    const [isToggled, setIsToggled] = useState(initialState);

    const handleToggle = () => {
        setIsToggled(!isToggled);
        if (onToggle) {
            onToggle(!isToggled);
        }
    };

    return (
        <div style={{ display: 'flex', alignItems: 'center' }}>
            <label style={{ marginRight: '10px' }}>{label}</label>
            <div
                onClick={handleToggle}
                style={{
                    cursor: 'pointer',
                    width: '50px',
                    height: '25px',
                    backgroundColor: isToggled ? '#4caf50' : '#ccc',
                    borderRadius: '25px',
                    position: 'relative',
                    transition: 'background-color 0.2s'
                }}
            >
                <div
                    style={{
                        position: 'absolute',
                        top: '50%',
                        left: isToggled ? '25px' : '0',
                        transform: 'translateY(-50%)',
                        width: '20px',
                        height: '20px',
                        backgroundColor: 'white',
                        borderRadius: '50%',
                        transition: 'left 0.2s'
                    }}
                ></div>
            </div>
        </div>
    );
};

export default ToggleSwitch;
```

### Usage Example

```jsx
import React from 'react';
import ReactDOM from 'react-dom';
import ToggleSwitch from './ToggleSwitch';

const App = () => {
    const handleToggleChange = (newState) => {
        console.log('Toggle is now:', newState);
    };

    return (
        <div style={{ padding: '20px' }}>
            <h1>Toggle Switch Example</h1>
            <ToggleSwitch label="Enable Feature" onToggle={handleToggleChange} />
        </div>
    );
};

ReactDOM.render(<App />, document.getElementById('root'));
```

### Explanation
- **Reusable Component**: The `ToggleSwitch` component can be reused throughout your application with varying labels and initial states.
- **Customizable Callback**: It accepts an `onToggle` prop to handle toggle changes, enabling easy integration with other parts of your application.
- **Visual Feedback**: The toggle switch provides visual feedback with color changes to indicate the on/off state, improving user experience.
- **Styled and Accessible**: Simple inline styles ensure the component is visually appealing and accessible to users.

Feel free to customize the styles further or expand its functionality as needed!
