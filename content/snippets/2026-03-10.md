// Generated at: 2026-03-10T04:44:37.745Z

This snippet demonstrates how to create a reusable button component in React with customizable styles and behavior.

### `CustomButton.js` - Reusable Custom Button Component

```jsx
import React from 'react';
import PropTypes from 'prop-types';

const CustomButton = ({ label, onClick, style, disabled }) => {
  return (
    <button 
      onClick={onClick} 
      style={style} 
      disabled={disabled} 
      className="custom-button"
    >
      {label}
    </button>
  );
};

CustomButton.propTypes = {
  label: PropTypes.string.isRequired,
  onClick: PropTypes.func.isRequired,
  style: PropTypes.object,
  disabled: PropTypes.bool,
};

CustomButton.defaultProps = {
  style: {},
  disabled: false,
};

export default CustomButton;
```

### Usage Example

```jsx
import React from 'react';
import CustomButton from './CustomButton';

const App = () => {
  const handleClick = () => alert('Button clicked!');

  return (
    <div>
      <h1>My App</h1>
      <CustomButton 
        label="Click Me!" 
        onClick={handleClick} 
        style={{ backgroundColor: 'blue', color: 'white', padding: '10px 20px' }} 
      />
      <CustomButton 
        label="Disabled Button" 
        onClick={handleClick} 
        disabled 
        style={{ backgroundColor: 'gray', color: 'darkgray', padding: '10px 20px' }} 
      />
    </div>
  );
};

export default App;
```

### Explanation
- **Reusable Component**: The `CustomButton` component can be easily reused throughout your application with different labels, click handlers, and styles.
- **Customizable Props**: Accepts `label`, `onClick`, `style`, and `disabled` props to give flexibility in usage.
- **Prop Types**: Utilizes PropTypes for type checking, improving code robustness and maintainability.
- **Default Props**: Provides sensible defaults for optional props like `style` and `disabled` to enhance usability.

This reusable button component can be integrated into any React application to streamline button creation and styling.
