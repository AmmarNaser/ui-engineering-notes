// Generated at: 2026-03-03T04:49:41.660Z

This snippet demonstrates how to create a reusable button component in React that can be customized with different styles and actions. 

### `Button.js` - Reusable Button Component

```javascript
import React from 'react';
import PropTypes from 'prop-types';

const Button = ({ label, onClick, style, disabled }) => {
  return (
    <button
      onClick={onClick}
      style={style}
      disabled={disabled}
      className="custom-button"
    >
      {label}
    </button>
  );
};

Button.propTypes = {
  label: PropTypes.string.isRequired,
  onClick: PropTypes.func.isRequired,
  style: PropTypes.object,
  disabled: PropTypes.bool,
};

Button.defaultProps = {
  style: {},
  disabled: false,
};

export default Button;
```

### Usage Example

```javascript
import React from 'react';
import ReactDOM from 'react-dom';
import Button from './Button';

const App = () => {
  const handleClick = () => {
    alert('Button clicked!');
  };

  return (
    <div>
      <h1>Hello, World!</h1>
      <Button 
        label="Click Me" 
        onClick={handleClick} 
        style={{ backgroundColor: 'blue', color: 'white' }} 
      />
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
```

### Explanation
- **Reusable Component**: The `Button` is designed to be reused with different labels, styles, and actions.
- **PropTypes Validation**: The component utilizes `PropTypes` for prop validation, ensuring that the correct data types are passed.
- **Default Props**: Default props are set for optional parameters, providing sensible defaults for `style` and `disabled` state.

This button component can easily be integrated into any React application and customized as needed.
