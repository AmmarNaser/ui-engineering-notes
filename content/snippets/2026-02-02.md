// Generated at: 2026-02-02T05:00:27.524Z

This snippet demonstrates a reusable React component that fetches and displays user data from a public API. It's designed to be easily integrated into any React application.

### `UserList.js` - Fetch and Display User Data
```javascript
import React, { useEffect, useState } from 'react';

const UserList = () => {
  const [users, setUsers] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchUsers = async () => {
      try {
        const response = await fetch('https://jsonplaceholder.typicode.com/users');
        if (!response.ok) throw new Error('Network response was not ok');
        const data = await response.json();
        setUsers(data);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };
    fetchUsers();
  }, []);

  if (loading) return <p>Loading...</p>;
  if (error) return <p>Error: {error}</p>;

  return (
    <ul>
      {users.map(user => (
        <li key={user.id}>
          {user.name} - {user.email}
        </li>
      ))}
    </ul>
  );
};

export default UserList;
```

### Usage Example
```javascript
import React from 'react';
import ReactDOM from 'react-dom';
import UserList from './UserList';

const App = () => (
  <div>
    <h1>User List</h1>
    <UserList />
  </div>
);

ReactDOM.render(<App />, document.getElementById('root'));
```

### Explanation
- **Data Fetching**: The component fetches user data from a public API using the `fetch` API and handles loading and error states.
- **Dynamic Rendering**: It dynamically renders a list of users including their names and emails once the data is loaded.
- **Reusability**: The `UserList` component can be easily imported and used in any other component of your React application.

This simple component can enhance your application by displaying real-time data with minimal configuration.
