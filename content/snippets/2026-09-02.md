// Generated at: 2026-09-02T07:44:26.849Z

This snippet demonstrates a custom React hook that manages form state, including validation and submission handling.

### `useForm.js` - Custom Hook for Form Management

```javascript
import { useState } from 'react';

const useForm = (initialValues, validate) => {
  const [values, setValues] = useState(initialValues);
  const [errors, setErrors] = useState({});

  const handleChange = (e) => {
    const { name, value } = e.target;
    setValues({ ...values, [name]: value });

    if (validate) {
      const validationErrors = validate(values);
      setErrors(validationErrors);
    }
  };

  const handleSubmit = (callback) => (e) => {
    e.preventDefault();
    if (Object.keys(errors).length === 0) {
      callback(values);
    }
  };

  return {
    values,
    errors,
    handleChange,
    handleSubmit,
  };
};

export default useForm;
```

### Usage Example

```javascript
import React from 'react';
import useForm from './useForm';

const validate = values => {
  const errors = {};
  if (!values.email) {
    errors.email = 'Email is required';
  }
  if (!values.password) {
    errors.password = 'Password is required';
  }
  return errors;
};

const MyForm = () => {
  const { values, errors, handleChange, handleSubmit } = useForm(
    { email: '', password: '' },
    validate
  );

  const onSubmit = (formValues) => {
    console.log('Form submitted:', formValues);
  };

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <div>
        <label>Email:</label>
        <input 
          type="email" 
          name="email" 
          value={values.email} 
          onChange={handleChange} 
        />
        {errors.email && <span>{errors.email}</span>}
      </div>
      <div>
        <label>Password:</label>
        <input 
          type="password" 
          name="password" 
          value={values.password} 
          onChange={handleChange} 
        />
        {errors.password && <span>{errors.password}</span>}
      </div>
      <button type="submit">Submit</button>
    </form>
  );
};

export default MyForm;
```

### Explanation
- **Custom Hook**: `useForm` encapsulates form logic, making it easy to manage and reuse across components.
- **Validation Support**: The hook accepts a validation function to check values and set errors dynamically.
- **State Management**: Manages both form values and errors using React's `useState` hook, ensuring smooth user experience.
- **Submission Handling**: Provides a clear way to handle form submission with either successful or failed validation states.

This custom hook makes form management in React simple and efficient.
