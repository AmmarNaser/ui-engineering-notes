# 2026-03-15

_Generated at: 2026-03-15T05:09:09.600Z_

### useEffect - Managing Side Effects in Functional Components

The `useEffect` hook is a key feature in React that allows developers to perform side effects in functional components. Side effects can include data fetching, subscriptions, or manually changing the DOM, and using `useEffect` helps manage these effects efficiently.

### When to Use It
- **Data Fetching**: Use `useEffect` to fetch data from an API when the component mounts.
- **Event Listeners**: Utilize `useEffect` to add or remove event listeners when the component mounts or unmounts.

### Example
```jsx
import React, { useState, useEffect } from 'react';

function ExampleComponent() {
  const [data, setData] = useState(null);

  useEffect(() => {
    fetch('https://api.example.com/data')
      .then(response => response.json())
      .then(data => setData(data))
      .catch(error => console.error('Error fetching data:', error));

    // Cleanup function (if needed)
    return () => {
      // Cleanup logic, e.g., removing subscriptions
    };
  }, []); // Empty dependency array means this runs once on mount

  return (
    <div>
      <h1>Fetched Data</h1>
      <pre>{JSON.stringify(data, null, 2)}</pre>
    </div>
  );
}

export default ExampleComponent;
```

### Explanation
- **Lifecycle Management**: `useEffect` runs after every render by default, allowing you to manage component lifecycle events effectively.
- **Dependency Array**: The second argument to `useEffect` (an array of dependencies) controls when the effect runs—providing an empty array makes the effect run only on mount and unmount.

In summary, `useEffect` is a powerful hook for managing side effects in functional components, enhancing the overall functionality and cleanliness of your React application.
