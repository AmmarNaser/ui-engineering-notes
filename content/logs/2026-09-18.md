# 2026-09-18

_Generated at: 2026-09-18T08:03:54.705Z_

### Custom Hooks - Creating Reusable Logic in React

Custom hooks are a powerful feature in React that allows developers to extract and reuse stateful logic from components, promoting cleaner code and component reuse.

### When to Use It
- **Repetitive Logic**: When you find that multiple components need to share the same stateful logic, such as form handling or fetching data.
- **Complex Components**: For components with complicated state or effects, custom hooks can help simplify and encapsulate that logic.

### Example
```javascript
import { useState, useEffect } from 'react';

// A custom hook for fetching data
function useFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchData = async () => {
      try {
        const response = await fetch(url);
        if (!response.ok) {
          throw new Error('Network response was not ok');
        }
        const result = await response.json();
        setData(result);
      } catch (error) {
        setError(error);
      } finally {
        setLoading(false);
      }
    };

    fetchData();
  }, [url]);

  return { data, loading, error };
}

// Usage in a component
function DataDisplayComponent({ apiUrl }) {
  const { data, loading, error } = useFetch(apiUrl);

  if (loading) return <div>Loading...</div>;
  if (error) return <div>Error: {error.message}</div>;

  return <div>Data: {JSON.stringify(data)}</div>;
}
```

### Explanation
- **Encapsulation of Logic**: Custom hooks allow you to encapsulate logic that relates to a specific behavior or feature, making your component code more concise and easier to read.
- **Reusability**: Once written, custom hooks can be reused across different components, reducing duplication and promoting consistency in how stateful logic is handled.

By leveraging custom hooks, you can significantly improve the maintainability and readability of your React applications.
